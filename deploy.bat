@echo off
echo ==========================================
echo [1/3] 게임 빌드 시작 (pygbag)
echo ==========================================
:: 🚩 파이썬 버전 3.12/3.13 대응을 위해 py -m 사용
py -m pygbag --build --title vampire_v4 .

echo.
echo ==========================================
echo [2/3] vampire-web 폴더로 파일 배달 중...
echo ==========================================
:: 🚩 현재 위치에서 상위 폴더의 vampire-web 폴더를 찾아서 복사
:: /s /e /y /i 옵션은 "묻지도 따지지도 말고 다 덮어써라"는 뜻
xcopy "build\web\*" "..\vampire-web\" /s /e /y /i

echo.
echo ==========================================
echo [3/3] 깃허브 업로드 (vampire-web 저장소)
echo ==========================================
:: 🚩 배포용 폴더로 이동해서 깃 명령어 실행
cd ..\vampire-web

:: 🚩 혹시 모르니 브랜치를 main으로 강제 고정하고 푸시
git add .
git commit -m "Auto Update: %date% %time% Supabase OK"
git push origin main

:: 🚩 다시 원래 폴더로 복귀
cd ..\vampire_survivor_v3

echo.
echo ==========================================
echo [완료!!] 이제 1분 뒤에 링크 확인해봐!
echo https://202510404-alt.github.io/vampire-web/
echo ==========================================
pause